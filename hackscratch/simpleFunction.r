# Try running this program from console as in square(3) and you will get an error
# But if you copy this function then it works



square <- function(number)
  {
    answer <- number * number
    return(answer)
  }

# Also, the following call to function works as well

square(12)